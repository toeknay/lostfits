{
  auto_https off
  admin off
}

:80 {
  encode zstd gzip

  @api path /api* /docs* /openapi.json
  handle @api {
    reverse_proxy api:8000
  }

  root * /srv/www
  file_server
  try_files {path} /index.html

  log {
    output stdout
    format console
  }
}
