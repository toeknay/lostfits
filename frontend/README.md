# LostFits Frontend (Vue 3 + Vite + TS)

## Dev
```bash
cd frontend/vue-app
npm install
npm run dev
```
Dev server: http://localhost:5173

## Build
```bash
npm run build
```
Build output goes to `frontend/vue-app/dist/` (served by Caddy in docker-compose).
